import java.util.*;
public class CountNOnrepeatedchar {
    public static int countNonrepeatedchar(String str){
      //  int count=0;
        HashMap<Character,Integer> map=new HashMap<>();
        Set<Character> set=new HashSet<>();
        char[]ch=str.toCharArray();
        for(char key:ch){
            if(map.get(key)==null){
            map.put(key,1);
        }
        else{
            map.put(key,map.get(key)+1);
        }
        }

        // for(char key:str.toCharArray()){
        //     if(map.get(key)==2){
        //         set.add(key);
        //     }
        // }

       // return set.size(); //size return

        // StringBuilder result = new StringBuilder();//  for value print 
        // for (char c : set) {
        //     result.append(c);
        // }

        // return result.toString();

        

    return ;

    }

    public static void main(String[] args) {
        String str="abceead";
        System.out.println(countNonrepeatedchar(str));

    }
    
}













































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































